---
// CAPTCHA disabled. Nothing is rendered — the honeypot field in AuthCard.astro
// still provides basic bot protection. Swap in a provider later by re-running the
// installer: `astro-users add auth --captcha cloudflare` (or `--captcha google`).
---
